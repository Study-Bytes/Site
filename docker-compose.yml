services:
  studybytes:
    build: .
    container_name: studybytes-app
    expose:
      - "3000"
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    networks:
      - proxy
networks:
  proxy:
    external: true
